apiVersion: apps/v1
kind: Deployment
metadata:
  name: neuron-deployment
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: neuron
          env:
            - name: ENVIRONMENT
              value: "development"
