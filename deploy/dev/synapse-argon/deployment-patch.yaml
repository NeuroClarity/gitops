apiVersion: apps/v1
kind: Deployment
metadata:
  name: synapse-argon-deployment
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: synapse-argon
          env:
            - name: ENVIRONMENT
              value: "development"
