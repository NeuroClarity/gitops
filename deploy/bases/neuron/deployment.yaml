apiVersion: apps/v1
kind: Deployment
metadata:
  name: neuron-deployment
spec:
  selector:
    matchLabels:
      app: neuron
  template:
    metadata:
      labels:
        app: neuron
    spec:
      containers:
        - name: neuron
          image: neuron
          resources:
            requests:
              cpu: "3500m"
              memory: "4Gi"
            limits:
              cpu: "3500m"
              memory: "6Gi"
          ports:
            - containerPort: 5000
