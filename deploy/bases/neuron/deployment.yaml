apiVersion: apps/v1
kind: Deployment
metadata:
  name: neuron-deployment
spec:
  selector:
    matchLabels:
      app: neuron
  template:
    metadata:
      labels:
        app: neuron
    spec:
      containers:
        - name: neuron
          image: neuron
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "500m"
              memory: "2Gi"
          ports:
            - containerPort: 5000
